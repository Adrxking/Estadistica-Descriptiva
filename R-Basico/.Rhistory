tinytex::install_tinytex()
install.packages("tidyverse")
library(tidyverse)
setwd("D:/Adrian/Developer/IA/Estadistica Descriptiva/R-Basico")
x = seq(3,50, by = 3.5)
x
x[2]
x[1]
x[length(x)]
x[length(x)-1]
# Obtener del 4 al 8
x[4:8]
x[seq(2, length(x), by = 2)]
# Obtener los impares
x[seq(1, length(x), by = 2)]
# Obtener los 3 últimos
x[(length(x)-3):length(x)]
# Obtener la columna 1, 5 y 6
x[c(1,5,6)]
x[x>30]
# Obtener entre 20 y 40
x[x>20 & x<40]
# Obtener todos menos 3 y 17
x[x!=3 & x!=17]
# Obtener mayores e iguales que 10
x[!x<10]
x[x%%2==0]
# Obtener los impares
x[x%%1==0]
# Obtener los impares
x[x%%2==1]
# Usar como condición otro vector siempre y cuando sean igual de largos
x = c(1,3,5,7,9,11)
y = c(2,4,6,8,10,12)
x[y>0]
x[y>10]
x[y>=10]
which(x>4)
# Obtener las columnas donde en x hay mayores que 6 y menores que 10
which(x>6 & x<10)
# Obtener la posición del número mas pequeño
which(x == min(x))
# Obtener la posición del número mas grande
which(x == max(x))
# Obtener la primera posicion del número mas pequeño
which(min(x))
# Obtener la primera posicion del número mas pequeño
which.min(x)
# Obtener la primera posicion del número mas grande
which.max(x)
